name: CI/CD Pipeline

on:
  push:
    branches:
      - develop

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Run deployment script
        run: |
          ls -l
          cd ../../scripts
          chmod +x deploy-development.sh
          ./deploy-development.sh
